on:
  push:
    paths-ignore:
      - "**/*.md"
      - "img/**"
  pull_request:
    paths-ignore:
      - "**/*.md"
      - "img/**"
  workflow_dispatch:

jobs:
  build:
    uses: zmkfirmware/zmk/.github/workflows/build-user-config.yml@main

# on: [workflow_dispatch]

# jobs:
#   build:
#     uses: zmkfirmware/zmk/.github/workflows/build-user-config.yml@main
